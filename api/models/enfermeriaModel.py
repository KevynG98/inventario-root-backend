def default_row_values():
    """
    Stub used by the initial migration for legacy enfermería tables.
    The actual models were removed from the codebase, but the migration
    still imports this helper to populate JSON defaults.
    """
    return {}
